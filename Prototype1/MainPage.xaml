<Page
    x:Class="Prototype1.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Prototype1"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <StackPanel Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" x:Name="MainPanel">
        <Button Content="New Todo" Click="OnNewTodo" Margin="10" HorizontalAlignment="Center"/>
        <StackPanel x:Name="CreatePanel" Margin="10"></StackPanel>

        <ListView x:Name="TodoList">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="Padding" Value="0"/>
                    <Setter Property="Margin" Value="10"/>
                    <Setter Property="MaxWidth" Value="300"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>

        <StackPanel x:Name="TodoPanel">

        </StackPanel>
    </StackPanel>
</Page>
